# 当前 Web UI 评审与改进建议

> 评审日期：2026-06-08  
> 评审范围：当前 Vue Web UI 的主要工作台页面、配置页、FPA 预览页、历史页、授权页、提示词调试页  
> 评审方式：启动 Vite 前端后，通过 Playwright 观察关键路由在桌面与窄屏视口下的真实渲染和页面文本  
> 本文性质：方案文档，不包含代码修改

## 结论摘要

当前 Web UI 的能力覆盖已经比较完整：生成入口、FPA 预览、配置管理、历史记录、授权、提示词调试都有明确页面，FPA 预览页也已经遵守结果审阅术语规范。

主要问题不是“界面不好看”，而是信息架构和操作层级还没有收拢：

- 首页首屏把主任务、执行监控、低频参数放在接近同等的视觉权重下，用户很难一眼判断当前最重要的动作是什么。
- 低频 FPA 参数过早暴露，占据了生成任务的主要视线。
- 后端未连接时，多个页面和多个模块重复出现 `请求失败 (500)`，错误态显得嘈杂。
- 配置页内容很全，但所有配置分区几乎同权铺开，更像长表单，不像可扫描、可定位的配置中心。
- FPA 预览页的术语正确，但局部卡片嵌套和空状态可以更紧凑，让审阅行为更靠近结果表格本身。

建议优先做“结构收口”，而不是先做大范围视觉换肤。

## 需要你一次性决策的事项

以下决策会影响后续实现方向。建议在开始代码修改前一次性确认，避免做到一半反复调整页面结构。

| 编号 | 决策点 | 推荐选择 | 备选选择 | 影响范围 |
|---|---|---|---|---|
| D1 | 首页主布局 | 选择“主操作区 + 运行监控区 + 折叠高级参数”的上下工作台结构 | 保留当前纵向卡片，只微调间距和按钮位置 | 影响 `Home.vue`、`ConfigPanel.vue`、`FpaRunSettingsSection.vue` |
| D2 | 低频 FPA 参数默认状态 | 默认折叠，标题显示当前方案摘要 | 默认展开 | 影响首页首屏密度和用户是否容易误改策略 |
| D3 | `预览 FPA 功能点` 的入口权重 | 保留在首页主操作区，但作为次级动作 | 从首页移除，只保留侧边栏 `FPA 预览` | 影响首页任务路径和 FPA 审阅入口发现性 |
| D4 | 后端离线错误展示 | 全局只显示一个主状态，局部模块显示安静空状态 | 每个模块继续显示自己的接口错误 | 影响 `AppShell`、`Config.vue`、`FpaPreview.vue` 的错误态 |
| D5 | 配置页组织方式 | 使用分区 tabs：常用 / FPA / 模板 / 高级 / 维护 | 使用左侧锚点导航，所有区块仍在同一页 | 影响 `Config.vue` 拆分方式和滚动体验 |
| D6 | 侧边栏“预览”结构 | 保留 `预览` 父级，下面挂 `FPA 预览`、`COSMIC 预览`、`SPEC 预览` | 只改成单个 `FPA 预览` 入口 | 影响导航清晰度和未来多生成器入口 |
| D7 | 普通用户是否看到提示词调试 | 本机模式展示，远程普通用户隐藏或放入高级工具 | 所有用户都展示 | 影响远程服务模式的信息复杂度 |

推荐一次性确认以下默认方案：

```text
D1：采用上下工作台结构
D2：低频 FPA 参数默认折叠
D3：首页保留 FPA 预览入口，但降为次级动作
D4：统一后端离线错误态
D5：配置页使用 tabs 分区
D6：侧边栏保留“预览”父级，下挂 FPA 预览 / COSMIC 预览 / SPEC 预览
D7：提示词调试作为高级工具，本机模式默认可见
```

如果以上默认方案都可以接受，后续实施就可以按本文“实施推进路线图”从阶段 1 开始。

## 评审观察

### 已观察页面

| 页面 | 路由 | 主要观察点 |
|---|---|---|
| 生成工作台 | `/` | 任务设置、执行监控、开始生成、低频任务设置 |
| 配置页 | `/config` | 环境诊断、AI 配置、运行默认值、FPA 策略、领域上下文、模板、备份 |
| FPA 预览页 | `/preview/fpa` | 输入设置、FPA 方案选择、基础数据生成、预览空状态、术语使用 |
| 历史页 | `/history` | 筛选条、空状态、后端失败提示 |
| 授权页 | `/license` | 授权状态、离线激活表单、错误态 |
| 提示词调试页 | `/prompt-debug` | 双栏调试结构、空状态 |

### 响应式观察

观察过的视口包括：

| 视口 | 结论 |
|---|---|
| `390 x 844` | 未发现横向溢出，内容会纵向堆叠 |
| `768 x 1024` | 未发现横向溢出，表单宽度可用 |
| `1280 x 720` | 未发现横向溢出，但首页主按钮位置更容易压在上下区块之间 |
| `1440 x 1000` | 配置页和首页空白/卡片层级问题最明显 |

响应式基础是可接受的，短期不需要把重点放在断点重写上。更值得先改的是页面层级、信息密度和错误呈现。

## 当前优点

### 1. 业务功能覆盖完整

当前路由已经覆盖 Web UI 的关键工作：

- 生成任务：`web_app/src/views/Home.vue`
- FPA 预览：`web_app/src/views/FpaPreviewPage.vue`、`web_app/src/components/FpaPreview.vue`
- 配置管理：`web_app/src/views/Config.vue`
- 历史记录：`web_app/src/views/History.vue`
- 授权：`web_app/src/views/License.vue`
- 提示词调试：`web_app/src/views/PromptDebug.vue`

这说明改进重点应是“让现有能力更易用”，不是推倒重做。

### 2. FPA 审阅术语基本正确

FPA 预览页已经使用以下规范术语：

- `新增/修改功能点`
- `类型`
- `生成方式`
- `计算依据归类`
- `计算依据说明`

这与 `docs/fpa/result-review-terminology.md` 保持一致。后续改 UI 时必须继续保留这些词，不要改回 `功能点类型`、`类型判定依据`、`功能点说明`、`说明详情` 等替代表述。

### 3. 页面已有基础设计系统

当前界面已经有统一的 CSS 变量、按钮样式、表单控件、surface 卡片、状态色和侧边导航。后续不需要引入大型 UI 组件库，也不应该用另一套视觉语言重写。

建议继续沿用现有变量和 Tailwind 组织方式，只调整布局结构、组件层级和状态表达。

## 核心问题

## P0：首页主任务路径不够清晰

### 现象

首页当前结构大致是：

1. `任务设置`
2. `执行监控`
3. `低频任务设置`

其中 `任务设置` 里又包含：

- 后端服务未连接提示
- 操作模式
- 功能清单路径
- FPA 预览入口
- 开始生成
- 任务状态

在桌面首屏中，`开始生成` 按钮横向铺开，位置靠近 `执行监控` 和 `低频任务设置` 的边界，视觉上像插在两个卡片之间。用户会看到很多可操作项，但不容易立刻判断“我现在要先做什么”。

### 影响

- 新用户会被 FPA 策略、运行参数、执行监控、提示词调试等低频信息分散注意力。
- `开始生成` 是主操作，但没有和输入路径形成足够紧密的行动区。
- 后端未连接时，用户看到禁用按钮和错误信息，但缺少一个清晰的下一步。

### 建议目标行为

首页首屏只强调一个主流程：

```text
选择生成内容 -> 填写功能清单路径 -> 开始生成 / 预览 FPA
```

执行监控只有在任务启动后才获得更高权重。低频参数默认收起，只在用户明确需要调整 FPA 方案、策略、规则集或输出目录时展开。

### 建议改法

1. 将首页改成左右两栏或上下两段的工作台结构：

| 区域 | 默认状态 | 说明 |
|---|---|---|
| 主操作区 | 展开 | 操作模式、功能清单路径、主按钮、FPA 预览入口 |
| 运行状态区 | 空状态轻量展示 | 未启动时只显示“等待任务启动”；运行后展开阶段进展 |
| 高级参数区 | 默认折叠 | 项目名称、输出目录、FPA 方案、执行策略、规则集、生成模式、清理旧输出 |

2. `开始生成` 应紧跟输入区，不要跨越整个页面压在区块之间。

3. `任务状态` 可以移动到运行状态区，不必在输入区重复显示。

4. `预览 FPA 功能点` 和 `开始生成` 要形成并列但有主次的动作：

| 动作 | 样式建议 | 说明 |
|---|---|---|
| 开始生成 | 主按钮 | 默认主路径 |
| 预览 FPA 功能点 | 次按钮或轻量链接按钮 | 审阅型路径，不应抢主按钮权重 |

### 涉及文件

- `web_app/src/views/Home.vue`
- `web_app/src/components/ConfigPanel.vue`
- `web_app/src/components/ActionBar.vue`
- `web_app/src/components/GenerationProgress.vue`
- `web_app/src/components/run/FpaRunSettingsSection.vue`

## P0：后端未连接错误态重复且噪声大

### 现象

当前只启动前端、不启动后端时，多个页面会显示：

- `后端未连接`
- `后端服务未连接`
- `请求失败 (500)`
- `无法加载 FPA 方案配置`
- `请确认后端服务已启动...`

配置页尤其明显，几乎每个配置卡片都显示一次 `请求失败 (500)`。

### 影响

- 用户会感觉系统整体坏掉，而不是“当前只是后端未启动”。
- 重复错误抢占了正常信息架构。
- `500` 是工程错误码，不是适合主界面反复展示的用户语言。

### 建议目标行为

后端不可用时，只保留一个强全局状态，局部模块进入安静的不可用态。

推荐表达：

```text
后端未连接
当前只能查看界面。启动后端服务后可运行任务、读取配置和生成预览。
```

局部模块只显示：

```text
等待后端连接后加载
```

不在每个卡片里重复显示 `请求失败 (500)`。

### 建议改法

1. 在 `AppShell` 或顶部状态区保留唯一的全局后端状态。

2. `Config.vue` 的配置区块在后端离线时显示统一空状态：

| 当前显示 | 建议显示 |
|---|---|
| 每块 `请求失败 (500)` | 页面顶部一个离线说明，配置区块显示“等待后端连接后加载” |

3. `FpaPreview.vue` 中 `无法加载 FPA 方案配置` 可以保留，但要降低视觉强度，并明确页面会使用内置默认方案。

4. `normalizeApiError` 可以继续保留原始错误，但只在排错详情、toast 或开发调试区域显示。

### 涉及文件

- `web_app/src/App.vue`
- `web_app/src/components/layout/AppShell.vue`
- `web_app/src/views/Config.vue`
- `web_app/src/components/FpaPreview.vue`
- `web_app/src/lib/api.ts`

## P1：配置页需要从长表单改成配置中心

### 现象

配置页当前从上到下铺开：

- 环境诊断
- AI 配置
- Web 与运行配置
- FPA 策略
- FPA 判定规则
- 业务规则
- 领域上下文
- Prompt 配置
- 模板配置
- 高级配置
- 配置备份
- 配置导入导出

内容完整，但视觉层级几乎相同，滚动距离长，用户要找某个配置时主要靠阅读和滚动。

### 影响

- 配置能力越多，页面越像“全部字段倾倒出来”。
- 高频配置和管理员级高级配置混在一起。
- 后端错误态下，页面被重复失败信息占满。

### 建议目标行为

配置页应该像配置中心：

```text
环境状态
常用配置
FPA 配置
模板与交付物
高级维护
备份与迁移
```

用户应能快速定位自己要改的是“AI Key / 默认输出 / FPA 方案 / 模板 / YAML 高级文件”中的哪一类。

### 建议改法

1. 页面顶部保留 `环境诊断`，但压缩为横向摘要条：

| 项 | 建议 |
|---|---|
| 后端连接 | 状态 badge |
| 工作模式 | 状态 badge |
| 模板目录 | 状态 badge |
| 配置接口 | 状态 badge |
| 最近检查 | 小字展示 |

2. 引入配置分区导航，推荐用 tabs 或左侧锚点：

| 分区 | 包含内容 |
|---|---|
| 常用 | AI 配置、运行默认值 |
| FPA | 方案与规则集、计算依据归类判定原则、领域上下文 |
| 模板 | 输出模板、下载模板、自定义输出模板 |
| 高级 | YAML / JSON 配置文件、Prompt 配置、业务规则 |
| 维护 | 备份恢复、导入导出 |

3. 高频表单保持展开，低频文本编辑、YAML、备份恢复默认折叠。

4. 每个分区只保留一个保存入口，避免多个 `刷新`、`保存` 按钮在同一视野里竞争。

### 涉及文件

- `web_app/src/views/Config.vue`
- `web_app/src/components/TemplateUpload.vue`
- `web_app/src/components/TemplateDownload.vue`

## P1：FPA 预览页应更像审阅工具

### 现象

FPA 预览页当前输入设置在左侧，右侧选择三级模块、FPA 方案、执行策略、规则集，然后生成基础数据和生成预览。页面空状态清晰，但右侧大面积空白较多，且 FPA 设置在左侧低频设置和右侧工具栏中都有出现。

### 影响

- 用户可能不确定应该先改左侧低频设置，还是右侧 FPA 方案。
- 预览结果尚未生成时，页面显得空而重。
- 结果出来后，如果每行说明都展开到表格中，审阅密度可能不足。

### 建议目标行为

FPA 预览页应该围绕“审阅结果”组织，而不是围绕“生成参数”组织。

推荐流程：

```text
选择输入 -> 生成基础数据 -> 选择三级模块 -> 生成预览 -> 审阅/确认/查看依据
```

### 建议改法

1. 左侧只保留输入来源和基础数据状态。

2. FPA 方案、执行策略、规则集合并到右侧顶部工具栏，不在左侧低频设置中重复出现。

3. 生成基础数据后，三级模块选择区域应显示模块数量、当前选择、功能过程数，让用户知道选择的审阅范围。

4. 预览结果区采用更紧凑的表格优先结构：

| 表头顺序 | 必须保留 |
|---|---|
| `#` | 是 |
| `新增/修改功能点` | 是 |
| `类型` | 是 |
| `生成方式` | 是 |
| `计算依据归类` | 是 |

5. `计算依据说明` 保持行内详情，不集中放到底部。

6. 需要人工确认的问题应放在结果上方的“待确认”区，并清楚区分：

| 类型 | 展示方式 |
|---|---|
| 当前运行确认 | 轻量确认面板 |
| 项目级经验沉淀 | 后续如果支持持久化，再提供明确的项目级保存动作 |

### 涉及文件

- `web_app/src/views/FpaPreviewPage.vue`
- `web_app/src/components/FpaPreview.vue`
- `web_app/src/components/run/FpaRunSettingsSection.vue`
- `web_app/src/composables/useFpaOptions.ts`

## P1：导航与页面命名可以更贴近任务

### 现象

当前侧边栏分为：

- 工作台：生成、预览、历史、配置
- 工具：授权、提示词调试

结构可用，但“预览”作为单个独立入口时，用户不容易看出这里会逐步承载 FPA、COSMIC、SPEC 等多个生成结果审阅入口；“提示词调试”作为普通工具入口对业务用户可能偏工程化。

### 建议改法

1. 侧边栏保留 `预览` 父级，下挂三个子入口：

```text
预览
- FPA 预览
- COSMIC 预览
- SPEC 预览
```

2. 页面内仍可保留 `FPA / COSMIC / SPEC` tabs，用于同一预览中心内的域切换和未来扩展。

3. 在 COSMIC / SPEC 预览能力完成前，不需要为了“已上线可见”而隐藏结构；本系统尚未上线，可以先按最终导航结构建设。

4. 配置页不采用同样的侧边栏子项拆法。配置保留一个 `配置` 入口，在页面内用 tabs 分成常用、FPA、模板、高级、维护。

5. `提示词调试` 可以保留在工具区，但对远程普通用户隐藏或放入高级工具。

6. `授权` 对本机用户是维护动作，不应该和日常工作台竞争注意力。

### 涉及文件

- `web_app/src/router/index.ts`
- `web_app/src/components/layout/AppShell.vue`

## P2：视觉系统可以更紧凑，但不建议大换肤

### 现象

当前整体视觉是浅色、低饱和、圆角卡片、浅青强调色。这个方向适合内部工具，但存在两个问题：

- 卡片太多时，页面像一串同权容器。
- 主色用于导航、按钮、预览入口、状态等多个用途，区分度有限。

### 建议目标

保持“安静、可靠、工具化”的基调，减少装饰感，让用户更快扫描任务状态和关键动作。

### 建议改法

1. 卡片只用于真正需要成组的内容，不把每个页面区块都做成同重量 surface。

2. 主按钮、导航选中、提示区使用不同强度，不都依赖同一种浅青背景。

3. 错误、警告、不可用状态减少大面积填色，优先使用轻量状态条和 badge。

4. 表单控件高度、label 间距、分组标题统一收紧，提升操作型页面的扫描效率。

## 推荐实施顺序

### 第一阶段：首页主流程收口

目标：让用户进入首页后立刻知道怎么开始。

修改范围：

- `Home.vue`
- `ConfigPanel.vue`
- `ActionBar.vue`
- `FpaRunSettingsSection.vue`

验收标准：

- 首屏只出现一个主操作路径。
- `开始生成` 不再压在卡片边界或低频设置上方。
- 低频任务设置默认折叠。
- 后端未连接时，主按钮禁用原因清楚，但不重复刷多个错误。

风险：

- 需要确认 `FpaRunSettingsSection` 是否同时被首页和 FPA 预览页复用，避免改首页时影响预览页。

### 第二阶段：统一后端离线错误态

目标：减少 `请求失败 (500)` 噪声，让离线状态更像系统状态而不是页面报错。

修改范围：

- `App.vue`
- `AppShell.vue`
- `Config.vue`
- `FpaPreview.vue`
- `lib/api.ts`

验收标准：

- 后端离线时，侧边栏或顶部只显示一个明确状态。
- 配置页不再每个区块都显示 `请求失败 (500)`。
- 原始错误信息仍可在排错详情或开发调试区域看到。

风险：

- 需要避免吞掉真实后端异常。建议保留开发调试信息，但改变默认用户可见文案。

### 第三阶段：配置页分区化

目标：把长配置页改成可定位的配置中心。

修改范围：

- `Config.vue`
- 必要时抽取配置分区组件

验收标准：

- 页面顶部有环境摘要。
- 常用配置、FPA、模板、高级、维护分区明确。
- 用户不需要滚动很久才能找到 AI Key、FPA 方案、模板配置。
- 后端离线时显示统一空状态。

风险：

- `Config.vue` 当前文件较大，直接改容易扩大 diff。建议先做布局层拆分，再迁移具体表单。

### 第四阶段：FPA 预览审阅体验优化

目标：让 FPA 预览页更像审阅工具。

修改范围：

- `FpaPreviewPage.vue`
- `FpaPreview.vue`
- `FpaRunSettingsSection.vue`

验收标准：

- FPA 参数只出现一处，不在左右两边重复。
- 生成基础数据、选择三级模块、生成预览的步骤更明显。
- 表格字段继续使用规范术语。
- `计算依据说明` 保持行级详情。

风险：

- FPA 审阅术语不能漂移。
- 确认流相关文案要区分当前运行确认和项目级沉淀。

## 验收清单

### 视觉与布局

- 桌面 `1440 x 1000`：首页首屏主任务路径清楚。
- 桌面 `1280 x 720`：主按钮不压在上下区块之间。
- 平板 `768 x 1024`：无横向滚动，低频设置折叠可用。
- 手机 `390 x 844`：无横向滚动，按钮文字不溢出。

### 业务术语

- FPA 页面继续使用 `新增/修改功能点`、`类型`、`计算依据归类`、`计算依据说明`、`生成方式`。
- 不出现 `功能点类型`、`类型判定依据`、`功能点说明`、`说明详情` 等替代表述。

### 错误态

- 后端离线时只有一个主状态提示。
- 配置区块不重复刷 `请求失败 (500)`。
- 真实 API 错误仍可追踪。

### 任务流

- 未启动任务时，执行监控是轻量空状态。
- 任务运行后，阶段进展和交付物操作显著可见。
- 完成后，打开交付物目录或下载 ZIP 是最突出动作。

## 不建议做的事

- 不建议先引入新的 UI 组件库。当前问题主要是信息架构，不是组件能力不足。
- 不建议做营销式首页。这个系统是工作台，不需要 hero 区、宣传文案或大面积装饰图。
- 不建议把所有设置都放进弹窗。低频设置可以折叠，但仍应可追踪、可复制、可排错。
- 不建议为了减少错误文案而吞掉错误。应该改变用户默认看到的层级，同时保留排错信息。

## 实施推进路线图

本路线图按“小步可验收”的方式推进。每一步都应单独提交，避免一个大改动同时改布局、文案、状态和组件拆分。

### 阶段 0：实施前基线确认

目标：建立可复现的 UI 基线，避免后续改动只靠肉眼判断。

修改范围：

- 不改生产代码。
- 可新增轻量检查脚本或临时记录，也可以只在实施记录中写明截图视口。

执行步骤：

1. 启动前端开发服务。
2. 打开以下路由：
   - `/`
   - `/config`
   - `/preview/fpa`
   - `/history`
   - `/license`
   - `/prompt-debug`
3. 在以下视口截图或人工确认：
   - `1440 x 1000`
   - `1280 x 720`
   - `768 x 1024`
   - `390 x 844`
4. 记录当前问题：
   - 首页主按钮位置。
   - 低频设置是否进入首屏。
   - 配置页错误态重复数量。
   - FPA 预览页是否出现术语漂移。

验收标准：

- 能稳定复现当前首页、配置页、FPA 预览页的问题。
- 后续每个阶段都有对比依据。

风险：

- 如果只启动 Vite 前端而不启动 FastAPI 后端，会看到离线态；这适合验证错误态，但不能替代完整联调。

建议提交：

```text
不一定提交；如果新增检查脚本，再单独提交。
```

### 阶段 1：首页主流程收口

目标：让首页首屏只服务一个主任务：选择输入并启动生成。

修改范围：

- `web_app/src/views/Home.vue`
- `web_app/src/components/ConfigPanel.vue`
- `web_app/src/components/run/FpaRunSettingsSection.vue`
- 必要时微调 `web_app/src/components/ActionBar.vue`

执行步骤：

1. 调整 `Home.vue` 布局，让页面顺序变成：
   - 主操作区
   - 运行监控区
   - 高级参数区
2. 在 `ConfigPanel.vue` 中保留主流程字段：
   - 操作模式
   - 功能清单路径
   - `开始生成`
   - `预览 FPA 功能点`
3. 从 `ConfigPanel.vue` 中移除或弱化 `任务状态`，把任务状态交给运行监控区表达。
4. 将 `FpaRunSettingsSection` 默认折叠。
5. 折叠标题显示当前 FPA 摘要，例如：

```text
高级参数：严格 FPA 口径 / AI 优先 / strict_fpa_rs / 审慎模式
```

6. 调整 `开始生成` 和 `预览 FPA 功能点` 的主次：
   - `开始生成` 使用主按钮。
   - `预览 FPA 功能点` 使用次按钮或轻量链接按钮。
7. 保持当前功能行为不变，不在这一阶段重写任务启动逻辑。

验收标准：

- 首页 `1440 x 1000` 首屏中，主操作路径清楚。
- 首页 `1280 x 720` 中，`开始生成` 不再压在两个区块之间。
- 未展开高级参数时，用户仍能启动默认生成。
- 展开高级参数后，原有 FPA 方案、执行策略、规则集、生成模式仍可配置。
- `390 x 844` 无横向滚动。

回归检查：

```powershell
cd web_app
npm run build
```

可选人工检查：

```text
/ 页面：主流程是否清楚
/ 页面：展开高级参数后控件是否完整
```

风险：

- `FpaRunSettingsSection` 也被 FPA 预览页复用，改默认折叠时要确认预览页不被误伤。
- 如果把 `任务状态` 完全移除，运行中的反馈可能变弱；建议只移动，不删除。

建议提交：

```text
feat(ui): simplify home generation workflow
```

### 阶段 2：统一后端离线错误态

目标：把“后端未连接”从多处报错改成一个全局状态，减少 `请求失败 (500)` 噪声。

修改范围：

- `web_app/src/App.vue`
- `web_app/src/components/layout/AppShell.vue`
- `web_app/src/views/Config.vue`
- `web_app/src/components/FpaPreview.vue`
- `web_app/src/lib/api.ts`

执行步骤：

1. 在 `AppShell` 的侧边栏或页面顶部保留唯一全局后端状态。
2. 定义离线态的用户文案：

```text
后端未连接
当前只能查看界面。启动后端服务后可运行任务、读取配置和生成预览。
```

3. 在 `Config.vue` 中，当 `config.backendStatus === 'offline'` 时：
   - 环境诊断显示离线摘要。
   - 各配置区块不再重复显示 `请求失败 (500)`。
   - 区块内容显示轻量空状态：`等待后端连接后加载`。
4. 在 `FpaPreview.vue` 中保留“使用内置默认方案”的提示，但降级视觉强度。
5. 保留原始错误信息给调试入口或 toast，不把错误直接吞掉。

验收标准：

- 只启动前端、不启动后端时，配置页不再多个区块重复 `请求失败 (500)`。
- 首页能明确告诉用户后端未连接导致无法生成。
- FPA 预览页仍能使用内置默认方案展示基础 UI。
- 后端真实异常仍能在调试或详情中追踪。

回归检查：

```powershell
cd web_app
npm run build
```

人工检查：

```text
只启动 Vite 前端：
- /config 不应满屏重复请求失败
- /preview/fpa 应说明使用默认方案

启动 FastAPI 后端：
- /config 应能正常读取配置
- /preview/fpa 应能正常加载 FPA 方案
```

风险：

- 错误态收口时容易过度隐藏真实问题。建议开发调试区域保留原始错误文本。
- `apiFetch` 如果改动过大，可能影响所有接口错误处理。优先从页面展示层收口。

建议提交：

```text
fix(ui): reduce duplicate backend offline errors
```

### 阶段 3：配置页分区化

目标：把长配置页改成可定位的配置中心。

修改范围：

- `web_app/src/views/Config.vue`
- 可选新增：
  - `web_app/src/components/config/ConfigSectionTabs.vue`
  - `web_app/src/components/config/ConfigStatusSummary.vue`
  - `web_app/src/components/config/ConfigSectionCard.vue`

执行步骤：

1. 先抽出顶部环境摘要：
   - 后端连接
   - 工作模式
   - 模板目录
   - 配置接口
   - 最近检查时间
2. 新增配置分区 tabs：

| tab | 内容 |
|---|---|
| 常用 | AI 配置、运行默认值 |
| FPA | FPA 策略、计算依据归类判定原则、领域上下文 |
| 模板 | 输出模板、下载模板、自定义模板 |
| 高级 | YAML / JSON 配置文件、Prompt 配置、业务规则 |
| 维护 | 配置备份、导入导出 |

3. 第一版只改变页面组织，不重写具体表单逻辑。
4. 保留各分区原有保存逻辑，避免一次改动同时迁移数据逻辑。
5. 分区切换时，避免未保存内容被无提示丢弃。
6. 如果当前已有未保存修改，切换 tab 时复用现有确认逻辑或提示用户。

验收标准：

- 用户能在 1 次点击内进入 AI 配置、FPA 配置、模板配置。
- 页面顶部始终能看到环境摘要。
- 后端离线时，每个 tab 都有统一空状态。
- `Config.vue` 没有因为视觉拆分导致保存逻辑失效。

回归检查：

```powershell
cd web_app
npm run build
```

人工检查：

```text
/config：
- 切换每个 tab
- 修改 AI 配置后保存
- 修改运行默认值后保存
- 修改 FPA 策略后保存
- 查看高级配置文件
```

风险：

- `Config.vue` 文件较大，直接大改容易引入回归。建议先做布局外壳，再逐步抽组件。
- tabs 方案会隐藏部分配置，需要确保搜索和定位成本降低，而不是让用户找不到入口。

建议提交：

```text
feat(ui): group configuration page into sections
```

### 阶段 4：FPA 预览审阅体验优化

目标：让 FPA 预览页更像审阅工具，而不是参数配置页。

修改范围：

- `web_app/src/views/FpaPreviewPage.vue`
- `web_app/src/components/FpaPreview.vue`
- `web_app/src/components/run/FpaRunSettingsSection.vue`
- `web_app/src/composables/useFpaOptions.ts`

执行步骤：

1. 左侧只保留输入来源、基础数据状态和必要说明。
2. FPA 方案、执行策略、规则集只在右侧顶部工具栏出现一次。
3. 基础数据生成后，三级模块选择器显示更多上下文：
   - 模块数量
   - 当前三级模块路径
   - 功能过程数量
4. 预览结果区采用表格优先。
5. 保持表头顺序：

```text
# / 新增或修改功能点 / 类型 / 生成方式 / 计算依据归类
```

实际用户可见文案必须使用：

```text
新增/修改功能点 / 类型 / 生成方式 / 计算依据归类 / 计算依据说明
```

6. `计算依据说明` 保持行级详情。
7. `needs_confirmation` 结果在表格上方显示“待确认”面板。
8. 区分当前运行确认和项目级沉淀：
   - 当前运行确认：只影响本次预览或生成。
   - 项目级沉淀：暂不默认保存，除非后续实现明确的保存动作。

验收标准：

- FPA 参数不在左右两边重复出现。
- 没有术语漂移。
- 用户能明确按顺序完成：输入 -> 基础数据 -> 三级模块 -> 生成预览 -> 审阅。
- 行级 `计算依据说明` 与当前行保持上下文关联。

回归检查：

```powershell
cd web_app
npm run build
```

术语检查：

```powershell
Select-String -Path web_app\src\components\FpaPreview.vue -Pattern '功能点类型|类型判定依据|功能点说明|说明详情'
```

风险：

- FPA 页面术语必须遵守 `docs/fpa/result-review-terminology.md`。
- 确认流 UI 若做得太重，会把预览页变成另一个配置页；建议先做轻量面板。

建议提交：

```text
feat(ui): improve fpa preview review workflow
```

### 阶段 5：导航命名与高级工具整理

目标：让侧边栏更贴近日常任务，把工程调试能力降到高级工具层。

修改范围：

- `web_app/src/router/index.ts`
- `web_app/src/components/layout/AppShell.vue`
- `web_app/src/views/PromptDebug.vue`
- 必要时涉及权限或工作模式判断

执行步骤：

1. 将侧边栏 `预览` 改为可展开父级。
2. 在 `预览` 下挂三个子入口：
   - `FPA 预览`
   - `COSMIC 预览`
   - `SPEC 预览`
3. 页面内仍保留 `FPA / COSMIC / SPEC` tabs，为未来扩展留位置。
4. 配置页保留单个 `配置` 入口，不拆成侧边栏子项；配置页内部使用 tabs 分区。
5. 将 `提示词调试` 放入高级工具分组。
6. 远程普通用户默认隐藏提示词调试入口；本机模式保留。
7. 授权入口保持在工具区，但视觉权重弱于日常工作台。

验收标准：

- 侧边栏日常路径更明确：生成、预览、历史、配置。
- `预览` 父级下能看到 `FPA 预览`、`COSMIC 预览`、`SPEC 预览`。
- 配置仍是单一入口，配置分区在页面内完成。
- 本机开发/维护人员仍能进入提示词调试。
- 远程普通用户不会被调试入口干扰。

回归检查：

```powershell
cd web_app
npm run build
```

风险：

- 侧边栏父子项需要清楚表达当前激活状态，避免用户进入 `FPA 预览` 后看不出自己仍在 `预览` 分组下。
- COSMIC / SPEC 预览入口可以先做壳和空态，但最终上线前必须补齐可用流程。
- 远程用户隐藏工具前，要确认是否已有业务用户依赖提示词调试。

建议提交：

```text
feat(ui): clarify navigation for fpa preview and advanced tools
```

### 阶段 6：视觉密度与一致性收尾

目标：在结构稳定后统一视觉细节，避免先换皮导致返工。

修改范围：

- `web_app/src/assets/main.css`
- 相关 Vue 组件中的 Tailwind class

执行步骤：

1. 统一表单控件高度、label 间距、分组标题间距。
2. 减少同权重 surface 卡片数量。
3. 统一状态 badge：
   - 成功
   - 警告
   - 错误
   - 离线
   - 等待
4. 检查按钮主次：
   - 页面主动作只保留一个主按钮。
   - 次动作使用次按钮。
   - 工具动作使用安静按钮或链接按钮。
5. 检查移动端按钮文字是否换行或溢出。

验收标准：

- 页面整体更紧凑，但不拥挤。
- 每个页面主按钮只有一个。
- 卡片不再层层套卡片。
- `390 x 844`、`768 x 1024`、`1280 x 720`、`1440 x 1000` 都无明显遮挡和溢出。

回归检查：

```powershell
cd web_app
npm run build
```

建议提交：

```text
style(ui): tighten web ui spacing and states
```

## 实施跟踪表

| 阶段 | 状态 | 依赖决策 | 主要文件 | 验收方式 |
|---|---|---|---|---|
| 阶段 0：基线确认 | 已完成 | 无 | 无生产代码 | Playwright 多路由、多视口观察 |
| 阶段 1：首页主流程收口 | 已完成：`7f25439` | D1、D2、D3 | `Home.vue`、`ConfigPanel.vue`、`FpaRunSettingsSection.vue` | `npm run build` + 首页视口检查 |
| 阶段 2：统一后端离线错误态 | 已完成：`ef463a6` | D4 | `AppShell.vue`、`Config.vue`、`FpaPreview.vue`、`lib/api.ts` | `npm run build` + `/config`、`/preview/fpa` 离线态检查 |
| 阶段 3：配置页分区化 | 已完成：`6c8cb1e` | D5 | `Config.vue` | `npm run build` + tabs 切换 + 多视口无横向溢出 |
| 阶段 4：FPA 预览审阅优化 | 已完成：`bb8dbcf` | D2、D3 | `FpaPreview.vue`、`FpaPreviewPage.vue` | `npm run build` + 术语检查 + 预览页多视口检查 |
| 阶段 5：导航与高级工具 | 已完成：`229f25a` | D6、D7 | `router/index.ts`、`AppShell.vue`、`SideNav.vue`、`PreviewLayout.vue` | `npm run build` + 侧边栏展开 + COSMIC/SPEC 路由检查 |
| 阶段 6：视觉密度收尾 | 已完成：`09dd5cc`、`3aee215`、`d3d2c96` | 前 5 阶段完成 | `PromptDebug.vue`、`PreviewLayout.vue`、`main.css`、`History.vue`、`License.vue`、`Config.vue` | `npm run build` + 8 个主路由 x 4 个视口无横向溢出 |

### 已完成提交摘要

| 提交 | 阶段 | 摘要 |
|---|---|---|
| `7f25439` | 阶段 1 | 首页改成主操作、运行监控、高级参数的工作台结构，低频 FPA 设置默认折叠。 |
| `ef463a6` | 阶段 2 | 增加全局后端离线状态，配置页和 FPA 预览页减少重复 `请求失败 (500)`。 |
| `6c8cb1e` | 阶段 3 | 配置页增加 `常用 / FPA / 模板 / 高级 / 维护` 分区 tabs。 |
| `bb8dbcf` | 阶段 4 | FPA 预览页移除左侧重复参数，补充模块摘要和“待确认”区。 |
| `229f25a` | 阶段 5 | 侧边栏增加 `预览` 父级与 FPA/COSMIC/SPEC 子入口，提示词调试进入高级工具。 |
| `09dd5cc` | 阶段 6 | 提示词调试页改为移动端上下堆叠，预览中心页内 COSMIC/SPEC 链接可跳转。 |
| `3aee215` | 阶段 6 | 增加统一 `status-badge` 和 `empty-state` 样式，收口历史、授权、配置页状态呈现。 |
| `d3d2c96` | 阶段 6 | 将提示词调试快捷测试动作降为次按钮，保留“发送给 AI”为唯一主动作。 |

### 阶段 6 最终验收记录

阶段 6 已完成最终验收。验收范围覆盖：

- 主路由：`/`、`/history`、`/license`、`/config`、`/preview/fpa`、`/preview/cosmic`、`/preview/spec`、`/prompt-debug`
- 视口：`390`、`768`、`1280`、`1440`
- 检查项：横向滚动、前端页面错误、主按钮数量

验收结果：

- `npm run build` 通过。
- 32 个路由/视口组合均无横向滚动。
- 32 个路由/视口组合均无页面错误。
- 常规主路由最多只保留一个 `.btn-primary` 主动作。

## 每阶段通用完成定义

每个阶段完成时必须满足：

1. 只提交本阶段相关文件。
2. `npm run build` 通过。
3. 如果涉及 FPA 页面，术语检查通过。
4. 如果涉及布局，至少检查 `390 x 844`、`1280 x 720`、`1440 x 1000`。
5. 最终回复说明：
   - 变更范围。
   - 行为变化。
   - 验证命令。
   - 提交哈希。

## 下一步建议

当前 D1-D7 和阶段 1-6 已全部完成，阶段 7 已完成两轮本机联调冒烟。后续继续推进时，建议从远程服务模式和发布取舍开始，而不是继续扩大视觉微调：

1. 若要进入远程服务模式，补一轮远程登录、上传文件、下载 ZIP 的端到端验收。
2. 若上线范围只包含 FPA，确认 COSMIC / SPEC 预览壳层保持“建设中”占位，不把它们描述为已完成能力。
3. 若上线前需要真实业务样例验收，用业务非空功能清单再跑一次完整生成，并留存输出清单。

这一步以真实后端联调和发布取舍为主，避免在 UI 结构已经稳定后继续扩大纯视觉改动。

## 阶段 7：上线前联调验收

### 第一轮本机联调冒烟

执行日期：2026-06-08

启动方式：

```powershell
.\.venv\Scripts\python.exe -m uvicorn web_app.server:app --host 127.0.0.1 --port 8091
```

验证范围：

| 项目 | 结果 | 说明 |
|---|---|---|
| `/api/health` | 通过 | 返回 `ok=true`，工作模式为 `local`。 |
| `/api/modes` | 通过 | 返回 6 个生成模式。 |
| `/api/web-config` | 通过 | 返回本机配置视图，`scope.mode=local`。 |
| `/api/fpa/options` | 通过 | 返回 4 个 FPA 方案。 |
| `/api/fpa/preview-modules` | 通过 | 使用 `tests/fixtures/功能清单-录入模板.xlsx` 解析出 3 个三级模块。 |
| `/config` 静态前端 + 后端 | 通过 | 页面显示环境诊断和模型与凭据，不再显示离线态。 |
| `/preview/fpa` 静态前端 + 后端 | 通过 | 页面填写测试夹具路径后可生成基础数据，显示模块摘要。 |
| `/api/run-local` + `/api/sessions/{session_id}` + `/api/history` | 通过 | 使用 `from-excel-gen-basedata` 启动任务，session 状态为 `done`，历史记录可查到同一 session。 |

本轮使用的是测试夹具 `tests/fixtures/功能清单-录入模板.xlsx`。它适合验证接口、页面和任务链路，但不是完整业务样例；本轮 `gen-basedata` 任务完成后没有产生交付物文件，后续仍需要用真实非空功能清单验证交付物操作。

### 第二轮本机 FPA 交付物冒烟

执行日期：2026-06-08

启动方式：

```powershell
.\.venv\Scripts\python.exe -m uvicorn web_app.server:app --host 127.0.0.1 --port 8092
```

输入与策略：

| 项 | 值 |
|---|---|
| 输入文件 | `tests/fixtures/功能清单-录入模板.xlsx` |
| 生成模式 | `from-excel-gen-fpa` |
| FPA 方案 | `strict_fpa` |
| FPA 执行策略 | `rules_only` |
| 输出目录 | 系统临时目录，验证后已清理 |

验收结果：

| 项目 | 结果 | 说明 |
|---|---|---|
| `/api/run-local` | 通过 | 返回 session，任务进入执行。 |
| `/api/sessions/{session_id}` | 通过 | session 状态为 `done`。 |
| FPA 交付物 | 通过 | `done_files` 包含 `FPA 工作量评估` 和 `FPA 审核副本`。 |
| 输出目录 | 通过 | 生成 `FPA工作量评估.xlsx`、`FPA工作量评估-check.xlsx`、中间 MD、audit trace 和运行日志。 |
| `/api/history` | 通过 | 历史记录可查到同一 session，状态为 `done`，交付物类型为 `local_dir`，`open_folder_available=true`。 |
| `/history` 页面 | 通过 | 页面可见本次 FPA 任务、完成状态和 `打开目录` 操作。 |

### COSMIC / SPEC 预览发布取舍

保持当前占位结构，不在上线前隐藏 COSMIC / SPEC 入口。

理由：

- D6 已明确采用 `预览` 父级，下挂 FPA / COSMIC / SPEC 的最终信息架构。
- 当前 `/preview/cosmic` 和 `/preview/spec` 页面均展示“预览能力待接入”的安静空状态，没有伪装成可用流程。
- 本系统尚未上线，保留入口有利于后续能力接入和导航稳定。

发布口径：

- 可以说：`COSMIC / SPEC 预览入口已预留，能力建设中。`
- 不应说：`COSMIC / SPEC 预览流程已完成。`
